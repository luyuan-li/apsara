{
  "name": "apsara",
  "private": true,
  "version": "0.1.0",
  "description": "Apsara — Mac AI desktop pet (飞天)",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:desktop": "pnpm --filter @apsara/desktop dev",
    "build:desktop": "pnpm --filter @apsara/desktop build",
    "lint": "echo \"lint placeholders — wire when apps land\""
  }
}
